{
  "name": "@cadmus/cli",
  "version": "0.1.0",
  "description": "Cadmus CLI — scaffold, run, and inspect agents.",
  "license": "MIT",
  "type": "module",
  "bin": {
    "cadmus": "./dist/cli.js"
  },
  "main": "./dist/cli.js",
  "files": [
    "dist",
    "templates",
    "README.md"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json && chmod +x dist/cli.js",
    "dev": "tsc -p tsconfig.json --watch"
  },
  "dependencies": {
    "@cadmus/kernel": "0.1.0",
    "tsx": "^4.19.2"
  },
  "devDependencies": {
    "@types/node": "^20",
    "typescript": "^5"
  }
}
